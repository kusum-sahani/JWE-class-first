// function selfPrint hello world
    console.log("Hello World");

// Calculate the sum
function addNumbers(firstNumber, secondNumber) {
       let sum = firstNumber + secondNumber;
       return sum;
}
    
let returnVal = sum(firstNumber, secondNumber);
    console.log( returnVal);
